# Profile, feed and post view — progress

Read `00-system-map.md` for what exists, `01-findings.md` for what is wrong
(ids), `02-plan.md` for the sequence. This file is the only place session
state lives; update it at the end of every sub-phase.

## Status

| Sub-phase | Root cause | Status | Commit | Notes |
|---|---|---|---|---|
| 1a | RC-1 takes grid gutter | **done 2026-09-08** | see git log `fix(profile): stop the takes grid` | removed `background:#000` + the no-op mobile block from `.takes-grid` (app/globals.css). Verified 0/1/2/5 takes and Relays→Takes at 1400 px and phone width; tsc 0 errors, lint 0 errors (166 pre-existing warnings), 223 tests pass |
| 1b | RC-2 policies / grants | **done 2026-09-08, applied to prod** | see git log `fix(profile): hide email from the browser` | migration `20260913_profile_phase1b_grants.sql` (table grant on profiles replaced by column lists for anon/authenticated; `takes_select` mirrors `posts_select_policy`). Client: `lib/profiles/columns.ts`, explicit columns in `useProfile` + `AuthProvider`, client insert no longer writes email. Verified: DB role checks, REST as anon (select=* → 401, select=email → 42501, id/username → 200), feed/profile/settings in browser as owner. Found decision 10 (private accounts not enforced server-side for posts or takes) |
| 1c | RC-3 loading/error signal | **done 2026-09-08** | see git log `fix(profile): show errors as errors` | `useProfile` finally only clears loading for the run that owns the abort controller (X-1); `useUserTakes`/`useRelayedTakes` get a cancelled flag + `refetch`, `useRelays` gets `error` + `refetch` + ownership guard; a failed profile lookup in those hooks throws instead of rendering as empty; profile tabs render `TabErrorState` (Couldn't load … / Try again) before the empty state (P-20); post + take pages split network failure (Couldn't load … / Try again) from not-found, incl. the first query's `{ error }` (V-48); `FeedBoundary` client wrapper gives the feed fallback a working retry (F-13); feed shows "Still loading — trying again…" when the 12 s auto-retry fires (F-31). Also finished the 1b leftover: client-side take visibility checks removed from `useUserTakes` and `/take/[id]` (RLS owns it). Browser-verified: no not-found flash feed → profile; Takes tab failure + retry; post page failure + retry; not-found on post + take; feed retry note. Not browser-verified: the take page failure branch (its data loads before a fetch patch can land; same code as the post page) |
| 1d | RC-4 unchecked writes | **done 2026-09-08** | see git log `fix(profile): check every write` | New `lib/reports.ts` `submitReport` is the one insert shape (matches the live table: `post_id` / `take_id` / `reported_user_id` / `details` separate from `reason`); used by PostCard, PostDetailModal, post page, TakePostCard, TakeDetailModal, take page, `useTakes.reportTake`, StudioProfile (user report). Discovered the `reports` table was empty: post reports wrote a non-existent `reported_post_id` (X-4); take-comment reports used a rejected type (X-5, fixed). Save/relay/follow toggles on takes check `{ error }`, revert and toast (TakePostCard, TakeDetailModal, take page, useTakes); post page + modal save/relay catch the throwing toggles and revert; delete + block failures toast on both pages, both modals and TakePostCard; collection deletes toast. Browser-verified: take report row lands (type take, take_id, reported user), post report row lands (type post, post_id), failed take save reverts with "Failed to save take" toast; test rows deleted afterwards |
| 1e | RC-5 in-flight guards | **done 2026-09-08** | see git log `fix(profile): one save or relay in flight` | `PostCard` save/relay get `savePending`/`relayPending` refs (same pattern as `useTileActions`) and re-notify the bus on revert; `StudioProfile.handleFollow` gets try/catch/finally + toast so a failed follow/unfollow re-enables the button. Browser-verified: triple-click Save → one POST, final state saved, undo → one DELETE; unfollow with the request forced to fail → "Failed to unfollow" toast, button enabled, still Following; DB checked afterwards (follow intact, no stray save) |
| 1f | RC-6 one mapper | **done 2026-09-08** | see git log `fix(profile): one post-to-card mapper for every opener` | New `lib/posts/toPostProps.ts` (`toPostProps` / `toModalPost`, `PostLike` input) replaces the six mappers: Feed, Explore, saved page, collections item page, StudioProfile (posts + relays). One `ModalPost` type in `components/feed/PostCard/types.ts` used by `ModalProvider` and `PostDetailModal` (their two `Post` copies deleted; `authorId` now required). `useProfile` posts query now selects collaborators / mentions / tags through `POST_RELATIONS_SELECT` so those reach the modal from the profile. `lib/utils/format.ts` `formatCount` replaces the copies in StudioProfile, TakePostCard, TakeCard ("1.2K"); `decodeHtmlEntities` → `stripHtml`; the five inline `toLocaleDateString` calls use `lib/utils/time` (`shortDate`, `fullDate`, `formatDate`, new `mediumDate` / `monthYear`); post page `getTypeLabel` → `getPostTypePhrase`. Visible consequences, all inside findings: modal avatar fallback is `/defaultprofile.png` everywhere (P-42), profile card dates from before this year gain the year via `shortDate`, counts ≥1000 read "1.2K" not "1.2k". Browser-verified: content warning overlay from a profile tile (temporary warning set on my own post, then removed), hashtags from a profile tile, journal modal from the profile, explore + saved pages load, no console errors. Left alone: `formatCount` copies in CommunityHeader / CommunityCard (outside this audit) |
| 1g | RC-7 modal history | **done 2026-09-08** | see git log `fix(profile): one history entry per modal` | `ModalProvider`: open pushes ONE entry stamped `{pqModal, pqId, pqReturn}` (spreading Next's own state); opening another while open uses `replaceState`; close calls `history.back()` when we own the entry (falls back to `replaceState` after a reload); popstate closes on the way out and re-opens the last item on Forward. New context field `modalReturnPath`; `ConditionalRightSidebar` and `MobileBottomNav` key on it so the right sidebar stays mounted and the active tab stays put under the modal (V-8, confirmed: `usePathname` does change under the modal). `dynamic()` imports get a backdrop + spinner `loading` (V-46). Tests: 5 cases in `components/providers/__tests__/ModalProvider.test.tsx` (jsdom's `history.length` after `back()` is not browser-like, so asserts use pathname + state). Browser-verified on the feed: open = +1 entry, second open = same length + swapped id, Escape → `/` with length unchanged, Back while open closes, Forward re-opens, sidebar present during the modal |
| 1h | RC-8 pub/sub + memo | **done 2026-09-08** | see git log `fix(profile): every surface hears the modal` | `ModalProvider` gains an author-block bus (`subscribeToAuthorBlocks` / `notifyAuthorBlocked`); `PostCard` and `PostDetailModal` (new `onAuthorBlocked` prop) fire it on a successful block. Subscribed now: Feed (deletes + blocks), Explore (local removal instead of a full refetch), saved page (post + take deletes, blocks), collections item page, StudioProfile (post deletes, take deletes, blocks, and save/relay updates kept as per-post overrides applied to the next modal open). `PostCard` memo also compares title / content / contentWarning / media / styling; the DM share modal gets live counts; Stream rows read the comment count from the engagement store. Browser-verified: save in the modal → close → reopen from the profile tile shows Unsave (V-11); edited title (via SQL, reverted) appears on the card after the 30 s focus refresh (F-16). Not browser-verified (would need a real delete or a real block, both destructive on prod): the delete and block fan-out — same subscription shape as the feed's existing, working delete path |
| 1i | RC-9 follow | **done 2026-09-08** | see git log `fix(profile): one follow path` | `followUserRecord(followerId, followingId, knownIsPrivate?)` + new `unfollowUserRecord` in `lib/hooks/useProfile.ts` are the only client writes to `follows` for the current user (used by the profile button, FollowersModal, the take page, RightSidebar, the takes feed; `useFollowRequests` keeps its owner-side accept/decline). `useProfile` fetches the viewer's follow status (any status) and both block directions in its one wave and exposes `viewerFollowStatus` / `viewerHasBlocked`; StudioProfile's two duplicate effects are gone and it keeps only per-profile overrides. Follower / following counts adjust locally on follow, unfollow and owner-unfollow-from-modal, with a `base` snapshot that drops the delta after a refetch (P-5). The accepted-request refetch runs once (P-8). Take page uses status (Follow / Requested / Following) and the request flow (V-23). `useFollowList` orders by `created_at desc` and pages per list key (one fetch on switch, P-13). Browser-verified: Follow on poet → Followers 2→3 instantly with one POST (no is_private lookup), Unfollow → 2 with one DELETE, DB clean afterwards; Followers → Following in the modal = one request, 3 rows |
| 1j | RC-10 enrichPost / stats | **done 2026-09-08, RPC applied to prod** | see git log `fix(profile): relays and collabs through enrichment` | `useRelays(username, viewerId)` selects relay rows then the posts through the full select + `enrichPost` + `fetchUserPostFlags` (counters, tags, viewer flags — P-3); `fetchCollaboratedPosts(userId, viewerId)` same (P-14); profile tiles pick the first *image* item via `tileImage()` and the Gallery filter uses it (P-22); `useProfile(username, viewerId, { ready })` waits for auth and refetches when the viewer changes (P-46); Posts stat goes through `formatCount`. Decision 2 approved ("apply"): `get_profile_counts` (SECURITY DEFINER, author totals) applied to prod and `useProfile` reads Posts / Admires from it in the same Promise.all (P-9); the client-side length/sum derivation is gone (falls back to length only if the RPC errors). Verified: anon REST call returns {posts:29, admires:23}; profile stats unchanged for hadi (29/23) and hii (2/1). Browser-verified: audio/video-first tiles show the type icon, no empty-src images; hello's relayed posts show 2/3 and 3/7 counts |
| 1k | RC-11 pagination | **done 2026-09-08** | see git log `fix(feed): page by cursor` | `useFeed` pages by keyset `(created_at desc, id desc)` with `.or(created_at.lt…,and(eq,id.lt…))` + `.limit()` (decision 9), de-duplicates appends by id (F-1); tab refocus fetches only rows newer than the top and prepends them — the list is never collapsed to page 0 (F-2); the 200-post cap now ends the feed instead of slicing the newest posts off (F-3); dead `total` count gone from the feed (F-5; the type keeps `total?` for explore). `TakesFeed`: deep-link scroll runs once per id (V-31); follow lookups only for authors not yet asked about, and `useTakesFollowing.checkFollowing` merges (V-30). `TakeDetailModal` metadata effect is cancellable and resets per take (V-29). Verified: both cursor filters against the live REST API (older-than row 10 → row 11 first; newer-than-top → none; newer-than row 10 → the 9 above); 2 new unit tests (append+dedupe with the `lt` cursor, refocus prepend with the `gt` cursor). NOT browser-verified: infinite scroll itself — the automation tab is hidden (`document.hidden`, no rAF, no IntersectionObserver callbacks), so the sentinel never fires there; it is unchanged code |
| 1l | RC-12 take player | **done 2026-09-08** | see git log `fix(takes): page and modal use the real player` | `TakePlayer` replaces the raw `<video>` on `/take/[id]` and in `TakeDetailModal`, fed by the full `Take` (sound, playback speed, filter, volumes) built by new `takeFromTableRow` + `TAKE_ROW_SELECT` (`sound:sounds!takes_sound_id_fkey` — the plain embed is ambiguous because `sounds.original_take_id` also points at takes); `TAKE_FILTER_STYLES` / `takeVideoStyle` moved from TakeCard into the hook (V-18). Both surfaces use `useMuted` / `useVolume` (V-20) and `useTrackTakeView` / `useTrackTakeImpression` with sources "page" / "modal" (V-22); `isPlaying` bookkeeping gone (V-21). The page keeps its 9:16 frame and its own mute button; the modal frame now follows the take's `aspect_ratio` via `TAKE_ASPECT_CLASS` (it used object-contain before, so this keeps the whole video visible). Browser-verified: hii's sepia take shows `filter: sepia(80%)` on the page and in the modal, mute + duration present, modal frame 4:3, no console errors (playback itself cannot run in the hidden automation tab). Still open: decision 3 (V-19) for the feed and page, which crop non-9:16 takes |
| 2a | RC-13 fetch volume + caching (+ F-4) | **done 2026-09-08** | see git log `perf(profile): keep tab data` | Tab hooks (`useUserTakes`, `useRelayedTakes`, `useRelays`, `useCollections`) take `{ enabled }`, fetch once per (user, viewer, attempt) and keep their data when the tab is left (P-11; re-entering the Takes tab = 0 requests); `useFollowList` `{ enabled: isOpen }` so the always-mounted FollowersModal fetches only when opened; `useCommunities` only when the About box renders (P-12); `useComments` `{ enabled }` — both modals fetch comments only when the Discussion panel opens (V-27; modal open = 0 requests); take page = one select with author/sound/tags/collaborators/mentions embeds + 3 viewer lookups (4 page-specific requests, was 11; V-25); unpin renumbers with one upsert (P-15); Gallery height estimates memoised per post, TruncatedContent strips once (F-26). Profile posts paginated: `useProfile` returns `hasMorePosts` / `loadMorePosts` (page 24, `buildProfilePostsQuery`), StudioProfile has a sentinel and keeps paging before showing an empty sub-view (P-10). Feed keeps a 5-minute in-memory snapshot (pages + scroll offset) per viewer, hydrates on remount and fetches only newer rows; deletes/blocks prune it (`removePosts`); scroll restore retries until the page is tall enough and survives strict-mode double effects; the snapshot is saved from a layout-effect cleanup because the router scrolls to top in the same commit (F-4). Verified: profile Takes tab re-entry 0 requests; followers modal 1 request on open; take page 4 page requests; modal 0 on open / comments on Discussion click; feed → profile → back = 10 cards instantly, no skeleton, scrollY restored to 1500. NOT done from the plan: F-24 (feed RPC to cut `*` + 8 embeds; a DB change) and folding collaborated posts into the profile's first-page flags call — carried to 2b/2c notes |
| 2b | RC-15 re-render storms | **done 2026-09-08** | see git log `perf(profile): stop remounting card media` | `PostCard`: the inner `AuthorHeader` / `ContentSection` components were declared inside render, so every state change (save, relay, reaction) remounted the whole card subtree — carousel position, video element, expanded text all reset (F-8). Hoisted to module-level `CardAuthorHeader` / `CardContentSection` taking props; the card body is unchanged. `StudioProfile`: `collaboratedPostIdSet`, `allPosts`, `filteredPosts` derived with `useMemo` instead of being rebuilt in a render IIFE (P-28); excerpts come from a module cache `excerptOf(id, content, length)` so `stripHtml` runs once per post, not per render (F-27). `useComments.fetchReplies` reads the existing `commentsRef` and no longer lists `comments` in its deps, so the callback identity is stable while comments load (V-55). `useTracking`: one shared `IntersectionObserver` (`observeVisibility`) for all view trackers instead of one per card. Verified in the browser: feed carousel stays at "2 / 4" across a Save → unsave re-render; /studio/hadi sub-views still filter (all 21, poems 2, journals 2, gallery 4, back to all 21, no false empty state). tsc 0, lint 0 errors, 229 tests. NOT done: F-28 (gallery column reflow) — needs a layout change, carried to Phase 4; folding collab-post flags into one call carried to 2c |
| 2c | RC-14 media loading | **done 2026-09-08** | see git log `perf(media): lazy video thumbnails` | New `components/feed/LazyVideoThumb.tsx`: a `<video>` used only as a still (PostCard media grid, Stream thumb, Gallery tile, PostDetailModal strip, take grid tiles without a poster) carries no `src` until one shared IntersectionObserver (200px margin) says it is near, then loads metadata only (F-25, V-28/P-23). Take grid `<img>` thumbnails are `loading="lazy" decoding="async"`; the feed take card sets `preload="none"` when it has a poster and `"metadata"` otherwise. New `lib/media/playback.ts` (`announcePlayback` / `onOtherPlayback`): VideoPlayer, AudioPlayer and TakePlayer all announce on play and pause on any other player's announcement, replacing VideoPlayer's private window event and AudioPlayer's module Set (F-33). Hidden carousel slides get `inert` (F-34). Poster plumbing for F-12: `thumbnail_url?` on `PostMedia` / `MediaItem`, mapped in `toPostProps`, passed to every `VideoPlayer` (card, carousel, modal, post page) — but NO column stores a post-video poster (verified live: neither `posts` nor `post_media` has one), so the play-first card branch stays unreachable until decision 11. Tests: `lib/media/__tests__/playback.test.ts`, `components/feed/__tests__/LazyVideoThumb.test.tsx`. Browser: feed carousel hidden slides `inert`, the only mounted `<video>` on the classic feed is the player with `preload="none"`; /studio/poet1 Takes grid = 5 tiles, none with a poster, none with a `src`, 0 clip fetches (was one metadata fetch per tile); no console errors. tsc 0, lint 0 errors, 231 tests |
| 2d | RC-17 CSS layers + dead code | **done 2026-09-08** | see git log `refactor(profile): one css layer per feed card` | **F-10**: the feed card had three layers — shared `.post` base in globals.css (also used by the feed take card and the error fallback, so it stays), `.pq-feed-card` overrides in post-card.css, and a styled-jsx block in Feed.tsx. The styled-jsx block moved verbatim into post-card.css as a "Home feed (classic view)" section (values are scoped to `.home-feed-modern` on purpose: explore/profile/community cards render radius 20 / menu 32px, the home feed 22 / 34px — both measured); the `.pq-feed-card` overrides that only repeated base values were deleted, the ones that differ (action-btn min-height 40 / gap 7 / ui font, action gap 0.15rem, media-grid gap 6px) stay; the three `.type-unified/.type-audio/.type-video` borders that the deleted shell used to override now use `var(--color-border-light)` directly; base `.actions` border-top uses the same token (identical rgba in the light theme). **V-50 / P-27 / F-11 dead CSS**: a scanner listed every class selector in globals.css, takes.css, studio.css, post-card.css, video-player.css with zero references in app/, components/, lib/ (ts+tsx, dynamic prefixes `pq-post-`, `pq-media-count-`, `take-comments-` excluded); 186 in-scope classes → 311 rules removed from globals.css (8541 → 6338 lines), 35 from takes.css (581 → 303), 1 from post-card.css. `.takes-grid` / `.take-grid-*` moved to takes.css (imported by TakePostCard). **P-30**: `.studio-avatar` lost its `border: 4px solid white !important`; the img is now `border-4 border-surface` (renders the same 4px white in light theme, follows the theme elsewhere); FollowersModal and the profile loading skeleton use `border-surface` instead of `border-white`. Kept because no existing token renders the same colour: `.studio-relay-card` gradient `#fdfcff→#f8f6fc`, `.studio-relay-footer` `rgba(255,255,255,.5)`, `bg-gray-300` in loading.tsx. About-box accent line untouched (decision 4). `.studio-works-grid` `break-inside` not added (no relay card fragments across columns on hadi's profile). **TS dead code (subagent)**: PostCard/PostMenu, DeleteConfirmModal, StyledTypeLabel, constants, BlockConfirmModal, index.ts deleted; `PostProps.image/audioDuration/videoDuration` and their branches removed (video poster is `videoMedia?.thumbnail_url`); VideoPlayer `durationLabel` prop gone; `useFeed` `communityId` gone; P-24 Minimal Stats block gone; P-26 unused icons + `accentClass` gone, unused `useFollow` helpers gone; V-53 `savesCount` gone, TakeCard heart burst now draws at the tap point (event passed through TakePlayer `onDoubleTap`), `useTakes` 130-line table fallback that duplicated `get_takes_feed` gone; V-12 unreachable `openAuthModal` branches gone; P-45 upload ext from MIME + old avatar/cover object deleted after a successful save. Kept, audit was wrong: `AppRouterInstance` (used by CollectionCardProps), `useSellerProfile` (3 importers), `checkFollowStatus`/`checkIsPrivate` (unit-tested). Kept, needs a call: the two `typeLabels` maps differ (`blog` vs `Blog`). **Acceptance**: computed-style snapshots (36 properties) of 30 feed selectors, 21 explore selectors and 14 profile selectors taken before and after — 0 differences on all three (one regression caught and fixed mid-way: the type-* borders). tsc 0, lint 0 errors, 231 tests. **Carried**: V-51 (~400 lines of mobile post/take modal `!important` overrides keyed on utility classes) to 2f where the modal is rebuilt; out-of-scope dead CSS the scanner also found (logo-*, story-preview-*, create-take-*, sound-picker-*, voice-note-*, community-*, animate-float-*) left for their own areas |
| 2e | RC-18 page/modal duplication, posts | **done 2026-09-08** | see git log `refactor(post): one body, one action row` | New `components/feed/post-detail/`: `palette.ts` (`getPostPalette`), `usePostDetailActions.ts` (reactions, save, relay, comments + composer state, options menu, delete / report / block / remove-collaborator flows — one copy of every handler), `postMenu.tsx` (`buildPostMenuItems`), `PostDetailHeader`, `PostBody` (journal header, location, Spotify, title, body, audio), `PostMediaGallery`, `ContentWarningOverlay`, `PostActionRow`, `DiscussionBody`, `PostDetailDialogs` (share, delete, remove-collab, block, report; exports `DELETE_POST_COPY` / `BLOCK_COPY`). `PostDetailModal.tsx` 1174 → 176 lines, `app/post/[id]/page.tsx` 1327 → 496 lines; the page maps its fetched row through `toModalPost` (V-52). **V-4 parity, the modal's behaviour wins everywhere**: type phrase via `PostTypeChip`; `next/image` avatar with the default-avatar fallback (Unsplash fallback gone); options menu = Edit/Delete for the owner, Remove-me-as-collaborator/Block/Report for signed-in visitors (the page's Share/Copy-link menu rows are gone — Share lives in the action row, copy link inside ShareModal); guest reaction opens the auth modal on the page too (was disabled); the page's Comment pill scrolls the discussion into view and focuses the composer (was a no-op); delete copy "Erase this from your studio?" and block copy "Close the door on @user?" on card, modal and page, all through `ConfirmationModal` (the two inline block dialogs are gone); "Sign in to comment" links carry `?redirect=` to the current path; the non-journal location line now shows on the page as well. After delete/block the page still leaves to `/`, the modal still closes and prunes lists. Visible page deltas that come with sharing the modal's markup: title 1.5/2.2rem (was 1.3/1.6), journal date 3xl/4xl (was 2xl/3xl), body 1.1rem (was 1.05), gallery arrows appear on hover, Roman-numeral captions, `next/image` media. Verified in the browser: modal on the feed (header chip, menu, body, action row, Discussion panel + composer); /post/[id] as visitor (h1, chip, action labels identical to the modal, Comment pill focuses the composer, menu Block/Report, block dialog copy) and as owner (Edit/Delete, no Relay, delete dialog copy, comment count); no console errors. tsc 0, lint 0 errors, 231 tests |
| 2f | RC-18 page/modal duplication, takes (+ V-51) | **done 2026-09-08** | see git log `refactor(takes): one detail hook` | **Shared flows** in `components/feed/post-detail/flows.ts`: `useDiscussion` (comments + composer + like/reply/remove), `useReportFlow`, `useBlockFlow`; `copy.ts` holds `DELETE_POST_COPY`, `DELETE_TAKE_COPY`, `BLOCK_COPY`. `DiscussionBody`, `ContentWarningOverlay` (`variant="video"` for takes) and the action row (now `DetailActionRow`, `kind` + `pickerVariant`) are kind-agnostic; `usePostDetailActions` runs on the flows. **Takes**: `lib/takes/interactions.ts` (`setTakeSaved` / `setTakeRelayed`, the one write path — the feed hook, the post card, the modal and the page all inlined these), `components/takes/take-detail/` `useTakeDetailActions` (save, relay, follow, reactions, discussion, delete, report, block, menu, tracking, mute/volume), `useTakeMetadata` (tags/collaborators/mentions in ONE request; the page passes its embedded row and makes none — the modal used five), `TakeDetailHeader`, `TakeVideoFrame`, `TakeDetailDialogs`. `TakeDetailModal.tsx` 680 → 119 lines, `app/take/[id]/page.tsx` 827 → 276. `TakeCard` and `TakePostCard` use the report/block flows and the shared copy/overlay; `useTakes.reportTake` and `TakeCard.onReport` are gone. **V-4 parity**: the take modal gains Follow (with Requested for private accounts) and Block; the page's Comment pill focuses the composer; relay is hidden for the owner on both (the modal used to show it disabled); guest reactions open the auth modal on both (were disabled); one delete copy and one block copy through `ConfirmationModal` (the page's and card's inline block dialogs are gone); `next/image` avatar with the default fallback; sign-in links carry `?redirect=`. The frame follows `aspect_ratio` on the page too, so a 4:5 or 16:9 take is no longer cropped to 9:16 there (page half of V-19; decision 3 still covers the feed); mute + duration are always visible on both (the modal's hover-only overlay is gone). **V-51**: the ~400-line `@media (max-width: 767px)` block in globals.css is rewritten (408 → 320 lines): every rule keys on a class the shared components set on purpose (`post-detail-avatar/author/name/time`, `is-dark`, `journal-date-row/-title`, `journal-meta-row`, `journal-divider`, `post-detail-title`, `post-media-gallery/-frame/-thumbs/-thumb`, `post-actions-bar .engage-pill/.post-action-icon`, `discussion-header/-title/-close/-list/-composer`, `content-warning-overlay/-show`, `[role="menu"]`, `pq-modal-backdrop/-card` on Modal, `pq-confirm-card` on ConfirmationModal) and is scoped to `.post-detail-modal` — which also fixes a 2e regression: the page had inherited the modal's fixed action bar and sticky header on phones. Three already-dead rules (`.mobile-discussion-btn`, `.post-tags-section`, the `.w-9/.w-11` and `input`/`w-[42px]` composer rules) are dropped; the 44px share/save targets and the 16px composer font are keyed again. Verified in the browser: /take/[id] as follower (Following, 4:5 frame, mute + 0:38 badge, action labels, Comment pill focuses composer, Block/Report, block copy, one `follows` request), the take modal from a profile grid (Follow, Discussion toggle, 4:5 frame, mute, Block/Report, panel + composer, one `takes` metadata request); 390px iframes: post page bar/header static, modal bar fixed, header sticky, radius 0, avatar 36px, title 1.3rem, discussion panel + composer fixed (one stale dev-server CSS chunk needed a touch of globals.css to recompile). tsc 0, lint 0 errors, 231 tests |
| 3 | profile UI/UX | **done 2026-09-08** | see git log `feat(profile): phase 3 ui/ux` | Change list of 20 approved as "do what is best for pinkquill"; decisions 4 (accent line: removed), 5 (Blog sub-tab: removed), 6 (cover: settings preview now 30% like the profile) taken. Done: P-16 entrance fade off the tab bar and the four tab bodies (header, stats, About keep the first-load fade); P-17 one `StudioSkeleton` (cover, avatar, name, stats, tab bar, three cards) used by `loading.tsx` and by StudioProfile while fetching — the full-screen spinner is gone; P-18 scroll-reveal removed, cards render visible (they stayed at opacity 0 whenever the observer never fired); P-36 `?tab=` / `?view=` written with native `replaceState` (keeps the modal's history state) and read on load; P-40 an invisible flex-1 slot holds the Commissions tab's width while `hasCommissions` is unknown; P-31 name/handle/tagline `truncate` (+ `min-w-0` on the info column), bio `whitespace-pre-line break-words`, settings `maxLength` 50 on display name and 60 on role/location/education/languages; P-32/P-34 the profile's inline block and report dialogs are gone — `ConfirmationModal` with `BLOCK_COPY` and the shared `ReportModal` via `useReportFlow({type:"user"})`; `ReportModal` itself gained `role="dialog"`, `aria-labelledby`, `useDialog` (Escape, focus trap, scroll lock) and `max-w-[calc(100vw-2rem)]`; X-3 `.studio-subtabs` shows a thin scrollbar under `(hover: none)`; P-44 `excerptOf` cuts at the last space; P-33 `.studio-touch-reveal` (opacity 1 under `(hover: none)`) on both pin buttons, the collection-item delete, the collection overlay and the gallery-tile overlay; P-34 `role="tablist"`/`role="tab"`/`aria-selected` on both tab rows, `pressKeys()` (role=button, tabIndex, Enter/Space) on every tile that opens the modal and on the Followers/Following cells; P-35 Follow and Message render for guests and open the auth modal; P-37 the Message label stays "Message" (it said "Following…" while opening); P-38 "Link copied" / "Couldn’t copy the link" toasts; P-42 `DEFAULT_AVATAR` replaces both Unsplash fallbacks; P-41 metadata description says PinkQuill (post layout too) and OG gets `url`, `type: "profile"` and the avatar as image; P-1/P-30 `.takes-grid` gap 4px/8px and tile radius 2px/8px like the Gallery sub-view; P-25 Blog sub-tab, view mode and its 93-line view block removed; P-39 settings cover preview `opacity-30`; P-30 About-box accent line removed. Verified on /studio/poet1 as a visitor: Following + Message, no accent line, two tab lists (5 + 5 tabs) with `aria-selected`, sub-tabs All/Gallery/Poems/Journals/Communities, 2 animated sections left (header/stats), 6 tiles and 2 stat cells with role=button, Poems → `?view=poems`, Takes → `?tab=takes`, takes grid gap 8px / radius 8px, name h1 truncates; ReportModal from the post page: dialog role + label, body scroll locked, Escape closes, max-width = viewport − 2rem. Not browser-verifiable in the hidden tab: the portalled profile menu (its position waits on rAF) and `(hover: none)` rules. tsc 0, lint 0 errors, 231 tests |
| 4 | feed UI/UX | **done 2026-09-08** | see git log `feat(feed): phase 4 ui/ux` | 13-item list approved ("go ahead"), incl. decisions 7 (sidebar keeps overlapping, expanded rail gets `shadow-xl`) and 3 (non-9:16 takes letterboxed with `object-fit: contain` in the takes feed). F-19: `FeedViewMenu` has `variant="rail"` (sticky at the top of the feed column at md — there is no room beside the 580px column below lg — and `lg:fixed lg:right-[296px]` beside the right sidebar) and `variant="header"` rendered by `MobileHeader` on `/` in its action row; the floating `fixed top-[60px]` pill is gone; `FeedFrame` renders the rail itself. F-20: `.safe-area-bottom` defined; `MainContent` bottom padding is `calc(5rem + env(safe-area-inset-bottom))`. F-22: `PostSkeleton` rebuilt on the `.post .pq-feed-card` shell with tokens (44px avatar, 3 lines, 3 + 2 action slots, `animate-pulse`); `FeedSkeleton`/`FeedSkeletonItems` hold every view's placeholders; `FEED_CONTAINER_CLASS` moved to `lib/feed-view/layout.ts` and the route's `loading.tsx` uses the same 580px column. F-23: `app/(feed)/page.tsx` and `loading.tsx` read the view cookie with `cookies()` and pass `initialViewId` → `FeedBoundary` → `Feed`, used until the provider reports `isReady`; `FeedViewMenu` takes the same override. F-15: `DetailActionRow` Save/Relay open the auth modal for guests instead of rendering disabled. F-35: the empty feed shows "Sign in to create" (auth modal) to guests. F-29: cards get `tabIndex`, an `Open <type> by <name>: <title>` label and Enter/Space (only when the card itself is focused, so inner links/buttons keep theirs); Stream rows and Gallery tiles drop `role="button"` (they wrap links and buttons) but stay focusable with the same keys. F-30: view-menu buttons 40px (rail, header pill and its radios), `.post-menu-btn` 40px in both layers, carousel dots a 20px button around the same 7px `::before` dot, gallery heart/save 40px, "Continue reading" 10px vertical padding (≈42px). F-31: the feed error state is an amber warning glyph + "Couldn’t load the feed / Check your connection and try again." with `role="alert"`; the auto-retry note is `role="status" aria-live="polite"`. F-35b: `.unified-post-title` clamps at 3 lines with `overflow-wrap: anywhere`, the community header's "Posted by" author truncates at 10rem, `useMinuteTick` (one shared interval) refreshes the card's time words every minute. Verified in the browser on `/`: one visible rail (`fixed`, right 296px, top 32px, 40px buttons; a second copy sits in a hidden Next pre-render container), no floating pill, card `tabindex=0` + label, Enter on a focused card opens the modal and pushes `/post/<id>`, `.post-menu-btn` 40px, dot 20px with 7px inner, title `-webkit-line-clamp: 3`, "Continue reading" 41.8px; 390px iframe: the header pill (40px) opens three radios, no floating pill, rail `display: none`, main padding-bottom 80px. The globals.css chunk was stale once more (touch + re-fetch). tsc 0, lint 0 errors, 231 tests |
| 5 | post/take view UI/UX | not started | — | same; decision 8 |

## How to resume

1. `git status` must be clean except `tsconfig.tsbuildinfo`.
2. Pick the first row above that is "not started" or "in progress".
3. Re-read that sub-phase in `02-plan.md`; confirm the findings it cites
   still reproduce (the `file:line` references were taken at commit 706a1ab
   and will drift as phases land).
4. Dev server: `npm run dev` (port 3000). Test users: `hadi` (owner, 1
   take), `hii` (2 takes), `poet1` (5 takes), `poet` (0 takes).
5. On finish: lint, typecheck, tests, browser check at desktop and 390 px,
   commit with the message given in the plan, update this table.

## Session log

- 2026-09-08 — Phase 4 done and committed (decisions 3 and 7 taken as recommended). Next: Phase 5 (post/take view UI/UX) — change list first; decisions 8, 10, 11 pending.
- 2026-09-08 — Phase 3 done and committed (user delegated the 20-item list and decisions 4/5/6). Next: Phase 4 (feed UI/UX) — one-sentence change list first; decisions 3, 7, 8, 10, 11 pending.
- 2026-09-08 — 2f done and committed. **Phase 2 complete.** Next: Phase 3 (profile UI/UX) — needs a one-sentence change list approved first; decisions 3, 4, 5, 6, 7, 8, 10, 11 still pending.
- 2026-09-08 — 2e done and committed. Next: 2f (takes page/modal; V-51 mobile overrides). The page now uses the modal's type sizes and gallery — flag for the Phase 5 UI/UX pass if the page should differ. Decisions still pending: 3, 4, 5, 6, 7, 8, 10, 11.
- 2026-09-08 — 2d done and committed. Next: 2e (page/modal duplication, posts). Decisions still pending: 3 (V-19), 4 (About-box accent line), 5 (Blog sub-tab), 6, 7, 8, 10, 11. Scanner for dead CSS classes: scratch script, re-run before 2f (`take-comments-*` and the modal overrides).
- 2026-09-08 — 2c done and committed. New decision 11 (post-video posters need a `post_media.thumbnail_url` column + composer frame capture; SQL drafted below, not applied). Next: 2d (CSS layers + dead code; needs decision 4). Decisions still pending: 3 (V-19), 4, 5, 6, 7, 8, 10, 11.
- 2026-09-08 — 2b done and committed. Next: 2c (media loading: V-28/P-23, F-25, F-12, F-33, F-34). Decisions still pending: 3 (V-19), 4, 5, 6, 7, 8, 10.
- 2026-09-08 — plan approved by the user; 1a done and committed.
- 2026-09-08 — 1b SQL shown, user replied "apply"; migration applied to prod and committed.
- 2026-09-08 — 2a done and committed. Owner profile first paint measured at 23 REST requests incl. 4 app-shell calls (was 14 profile-only in the audit; 19 profile-only now because collab posts get their own flags — fold into one flags call in 2b). Automation gotcha reminder: scroll events, IntersectionObserver and rAF do not run in the hidden Chrome tab; programmatic scrollTo/scrollY do.
- 2026-09-08 — 1l done and committed. **Phase 1 complete.** Next: Phase 2a (fetch volume + caching + F-4 feed scroll restore). Decisions still pending: 3 (V-19), 4, 5, 6, 7, 8, 10.
- 2026-09-08 — 1k done and committed. Automation note: the Chrome tab used for checks is hidden, so anything driven by IntersectionObserver or requestAnimationFrame (infinite scroll, scroll-reveal, video autoplay) cannot be observed there — verify those via REST + unit tests, or ask the user.
- 2026-09-08 — 1j complete: user approved decision 2, `get_profile_counts` applied to prod and wired.
- 2026-09-08 — 1i done and committed. Test side effect: one follow + unfollow of `poet` by `hadi` (a follow notification to poet may remain).
- 2026-09-08 — 1h done and committed.
- 2026-09-08 — 1g done and committed.
- 2026-09-08 — 1f done and committed. Rule: any new list that opens the post modal or renders PostCard goes through `toPostProps` / `toModalPost`; never hand-build the shape.
- 2026-09-08 — 1e done and committed.
- 2026-09-08 — 1d done and committed. Open item outside this audit: the community mod queue (`useModQueue`, `ModQueuePage`) still reads `reported_post_id` (X-4).
- 2026-09-08 — 1c done and committed. Browser trick for failure states: patch `window.fetch` to reject a `/rest/v1/<table>?` URL right after navigation, then restore it and click Try again. New column on `profiles` = add it to the GRANTs and `lib/profiles/columns.ts`.

## Decisions log

| # | Question (see plan) | Answer | Date |
|---|---|---|---|
| 1 | 1b DB changes | approved ("apply") | 2026-09-08 |
| 2 | 1j `get_profile_counts` RPC | approved ("apply") | 2026-09-08 |
| 9 | 1k cursor ordering (created_at, id) | applied as planned (no objection) | 2026-09-08 |
| 4 | About-box accent line | removed (user: "do what is the best for pinkquill") | 2026-09-08 |
| 5 | Blog sub-tab | removed | 2026-09-08 |
| 6 | cover opacity | settings preview matches the profile (30 %) | 2026-09-08 |
| 3 | non-9:16 takes | letterboxed in the black 9:16 frame (feed, modal, page) | 2026-09-08 |
| 7 | sidebar hover | keeps overlapping; expanded rail casts `shadow-xl` | 2026-09-08 |
| 8, 10, 11 | — | pending (11 = post-video poster column + composer capture, drafted in 2c) | — |

# Progress and handoff

Last updated: 2026-09-05.
Baseline code: `e509c5a` on `main`; working tree was clean at start.
Active program: the September 5 Pinkquill 2.0 brief, summarized in `README.md`.

## Status

| Phase | Status | Evidence |
|---|---|---|
| 0 — Planning baseline | Complete for planning scope | Direction, source/browser audit, phase plan, and route inventory saved in this directory |
| 1 — Design proof | 1A/1C delivered; 1B implemented with native keyboard check open | Connected local proof; `06-design-proof.md` |
| 2 — Foundations | Implemented and verified (2A tokens, 2B controls/overlays, 2C frames) | `07-foundations.md`; 21 React checks; six-theme reference |
| 3 — Navigation/feed | Complete: 3A shell, 3B Home/feed views, 3C Explore/tags implemented and verified | `07-foundations.md`; shell, feed, explore tests; Playwright guest shots |
| 4 — Creation/content/Takes | 4A post detail and 4B composer implemented and verified; 4C Takes not started | `07-foundations.md`; PostDetail + composer piece tests; browser checks |
| 5 — Communities/conversation | Not started | |
| 6 — Studio/collections/saved | Not started | |
| 7 — Commerce discovery/listings | Not started | |
| 8 — Bag/checkout/orders | Not started | |
| 9 — Dashboards/operations | Not started | |
| 10 — Account/support/system | Not started | |
| 11 — Whole-product verification | Not started | |

## Decisions established by the user

- Full redesign, including feed, studio, communities, commerce, commissions, orders, dashboards, and the rest of the app.
- Preserve current colors as the starting identity.
- Welcome all creative disciplines, talent, sports/movement, and ordinary self-expression; no professional or writer-only qualification.
- Avoid tech-site, editorial/magazine, and recognizable clone presentation.
- Fix UI/UX and unify the experience while preserving existing features; future extensibility belongs in the architecture.
- Break the work into resumable phases.

## Proposed choices to prove in Phase 1

- “Room for your kind of creativity” as an internal direction; existing “Show your colors” remains useful brand language.
- Existing UI fonts, quieter chrome, work/media supplying the visual energy, consistent person/work/action composition.
- Stable search/navigation rather than hover-dependent expansion.
- Shared product language with purpose-specific social, focused-task, and management layouts.
- Keep existing names/routes/theme preferences; do not revive the abandoned identity track's renames, theme removal, or new features.

These are proposed design choices, not user-approved final pixels. Routine local work can proceed; there is no pending permission request.

## Planning baseline completed

Direction, source/browser audit, phase plan, and all 85 route assignments are saved. The project README and historical master plan point to this active program. Earlier default/mobile/role verification limits remain historical observations; current proof evidence is separate below.

## Current batch — Phase 1 and first foundation slice

Date / baseline: 2026-09-05 / `e509c5a`.

- **1A:** four connected screens built: Home, community, studios, submitted-delivery order. Varied fictional creators include illustration, dance, music, poetry, skating, clay, acting, and casual expression.
- **1B:** desktop/mobile navigation, creation choices and partial draft form, action menu, notifications, short sheet, long form, local social actions, and both order decision paths implemented. Default/Noir and 360/390/768/1280 layouts checked. Native dialog Tab boundary remains inconclusive in browser automation; explicit handler passes JSDOM. Do not mark 1B fully verified yet.
- **1C:** layout, typography, palette, shape, media, navigation, overlay decisions and limitations recorded in `06-design-proof.md`. The proof's Noir experiment and Gallery initial layout do not replace production preferences.
- **2A-controls / 2B-Button:** new semantic control roles and real shared Button migration. Six supported theme identities retained; Cream/Sepia control contrast corrected. Button native semantics, variants, loading/disabled state, ref, and caller styles preserved. Remaining tokens, primitives, and frames are pending.
- **Files:** `prototype/` (isolated fictional fixtures), `06-design-proof.md`, `07-foundations.md`, `app/design-tokens.css`, `components/ui/button.css`, `components/ui/Button.tsx`, imports in `app/globals.css`, `components/ui/__tests__/Button.test.tsx`, reference builder and generated `foundations/`.
- **Review:** open `prototype/index.html` and `foundations/index.html` directly, or serve this directory on local port 4318. Screenshot evidence was inspected inline; no durable screenshot export is claimed.
- **Checks:** 13 prototype JSDOM behaviors passed; four React Button behaviors passed; TypeScript passed; targeted ESLint passed; actual app CSS compiled for reference; measured all-six-theme control contrast. See linked documents for exact scope and native-browser limitations.
- **Data:** no fixture connected to an account; no live publishing, message, upload, payment, or order mutation. No dependencies/schema/financial rules changed. No commit, push, or deployment performed.
- **Route ledger:** all 85 production routes remain assigned; none is marked fully redesigned based only on this shared-component slice.
- **Revert:** remove the two new global imports and restore Button for runtime rollback; prototype/doc/reference files are independent. Leave unrelated user or OS file changes alone.

## Batch 2B-overlays — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** shared only; no route status changes. `components/ui/overlay/useOverlayLayer.ts`, `overlay/Scrim.tsx`, `overlay.css`, `Modal`, `Sheet`, `ConfirmationModal`, `ActionMenu`, `ReportModal`; `app/design-tokens.css` shape/surface roles; `app/globals.css` import; reference builder + `foundations/`.
- **Findings addressed:** UX-07 (menu/dialog literals leaking into dark themes), UX-12 (behaviour tests replace label-only checks for overlays), the source-observed `Modal` aria-hidden ancestor, mixed scroll-lock ownership, Escape closing more than one layer, text-selection dismiss, ReportModal overflow on narrow phones.
- **Existing capabilities checked:** every Sheet consumer (orders, commissions, admin) keeps its props; ActionMenu API unchanged for 21 consumers; ConfirmationModal API unchanged for 20 consumers; PostDetailModal/TakeDetailModal receive the wide dialog.
- **Evidence:** 13 UI tests pass (`npx vitest run components/ui`), full suite result recorded below, `tsc` clean, ESLint clean on `components/ui`; browser: post menu, delete confirmation, post detail in Default and Noir at 1380px in the running dev app; reference page rebuilt.
- **Untested:** real-keyboard Tab boundary in a browser, screen reader, phone software keyboard, Sheet consumers in authenticated order flows (not exercised to avoid live mutations), the still-unmigrated overlays listed in `07-foundations.md`.
- **Decisions:** no new `Sheet` side-panel variant until 3A has a real consumer; menus close on Tab; confirmation focuses the safe choice.
- **Revert boundary:** see `07-foundations.md`.

## Batch 2C + 3A — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** every route that used the old shell now renders through `AppShell` (all section layouts, `AdminShell`, `/post/[id]`, `/take/[id]`); shell statuses in `04-route-coverage.md` are unchanged because page content is still old. Files: `components/layout/{shell.css,navigation.tsx,AppShell.tsx,DesktopRail.tsx,TopBar.tsx,MobileBottomNav.tsx,MobileMoreSheet.tsx,PageFrame.tsx,RightSidebar.tsx}`, `components/notifications/NotificationPanel.tsx` (container only), `components/search/{SearchBar,SearchDropdown,SearchResultItem}.tsx`, `components/ui/{Sheet.tsx,overlay.css}` (panel presentation), sticky offsets in feed/community/explore/marketplace/tag/saved/checkout/commission/take, `app/globals.css` (import, canvas, aura), deleted `LeftSidebar.tsx`, `MobileHeader.tsx`, `MainContent.tsx`.
- **Findings addressed:** UX-01 (hover rail, hidden search), UX-02 (phone Create choices), UX-08 (repeated offsets replaced by shell variables), UX-13 (My studio vs Seller Studio labelled and placed), plus the Unsplash fallback avatar in search.
- **Existing capabilities checked:** all destinations and routes preserved; badge counts (messages, notifications, bag) preserved; guest gating derived from `protected-paths`; notification data, mark-as-read, invites and follow requests untouched; search history/results logic untouched; Takes stays immersive; Messages keeps its own header.
- **Evidence:** 21 component tests, full unit suite, `tsc`, ESLint (warnings only); browser at 1380px signed-in; Playwright guest screenshots at iPhone 12 / 768 / 1280 for `/`, `/shop`, `/community` with no horizontal overflow; E2E result below.
- **Untested:** signed-in phone flows (Create sheet, More sheet) only under JSDOM; real keyboard traversal of the rail; theme persistence via the More menu (not exercised to avoid changing the account preference); Messages on desktop now has no top bar (rail only) — Phase 5C decides whether it gets one.
- **Decisions:** guests see all public destinations; phone bottom bar is Home / Explore / Create / Takes / More (profile moved into More and the top-bar avatar); no uppercase tracked eyebrow labels from the proof are carried into production (existing design rule).
- **Revert boundary:** the files above plus the layouts; `git checkout` of `app/**/layout.tsx`, `components/admin/AdminShell.tsx`, the two detail pages and restoring the three deleted layout components returns the old shell.

## Batch 3B — Home and the three feed views — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/` (Home). `components/feed/{Feed.tsx,FeedViewSwitch.tsx,ComposerPrompt.tsx,PostSkeleton.tsx,feed.css}` (new switch, prompt, skeleton, frame stylesheet; `FeedViewMenu.tsx` deleted), `components/layout/RightSidebar.tsx`, `components/layout/navigation.tsx` (image/video/music icons), `components/feed/{StreamView,GalleryView}.tsx` (minor), and the shared post-card rules in `app/globals.css` ("POST CARDS", actions bar, stream, gallery).
- **What changed:** Home renders inside `PageFrame` (690px Classic column, 800px Stream, full width Gallery). The floating icon-only layout pill became a labelled segmented control in an in-page toolbar with a sentence-case section heading ("From your creative world" / "What people are making" for guests). Signed-in people get a quiet composer prompt linking to `/create`. The shared `.post` card is now one 17px radius, one soft line, no shadow or hover lift, 40px avatar, 13px name, 12px meta, and a calm action row with 42px targets and a tinted pressed state; audio/video/carousel type overrides inherit it. Stream rows and Gallery tiles use the same tokens (no lift, no image zoom, 40px action targets, tint hovers). Loading, empty, error and end states are distinct: error is an alert with Try again; empty differs for guests (Sign in) and members (Share something). The right column lost gradient titles, rank numbers, the Unsplash fallback portrait and gradient follow buttons; it uses the shared Button and tokens.
- **Findings addressed:** UX-01 follow-through (layout control visible and labelled on every width), UX-10 (fixtures not needed: real content shows the family resemblance), the proof's "creator, work, quiet action row" composition carried into production, the `home-feed-modern` style-jsx override removed.
- **Existing capabilities checked:** three view IDs and persistence untouched (`FeedViewProvider`); reactions, comments, relay, share, save, post menu, content warnings, collaborators, community header variant, infinite scroll, auto-retry, delete subscription all preserved (PostCard logic untouched); Stream expand-in-place and Gallery tile actions preserved.
- **Evidence:** 6 feed tests (`components/feed/__tests__/feed.test.tsx`: switch radiogroup and arrow keys; guest vs member empty states; error alert with retry; classic column and end line; stream/gallery routing); full suite 229 passing; `tsc` clean; ESLint warnings only. Browser (signed in, 1380px): Classic, Stream and Gallery in Default and Noir; Playwright guest `/` at iPhone 12 / 768 / 1280 with no horizontal overflow.
- **Untested:** posts with images/video/audio in the redesigned card (the local feed is text-only; verify on a media-rich account or fixtures in Phase 4), studio/community pages that share `.post` (they inherit the new card; their own frames are Phases 5–6), keyboard traversal of tiles in the browser.
- **Decisions:** no greeting hero on Home; work starts after one toolbar row. Follow buttons in the aside are outline, not primary, so Create stays the only purple button in view.
- **Revert boundary:** the files above; `git checkout app/globals.css components/feed components/layout/RightSidebar.tsx components/layout/navigation.tsx` and restore `FeedViewMenu.tsx`.

## Batch 3C — Explore and tags — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/explore`, `/tag/[tag]`. `components/explore/ExplorePageContent.tsx` (rewritten on `PageFrame`/`PageHeader`), `app/tag/[tag]/page.tsx` (rewritten), `app/tag/[tag]/layout.tsx` (now renders inside `AppShell`; the route previously had no rail or bottom bar at all), new shared `components/ui/Tabs.tsx` + `components/ui/tabs.css` (tab row, chips, list rows), `components/search/SearchResultItem.tsx` (tag results now open `/tag/<name>`).
- **What changed:** Explore has a page title and one-line lede, a real `tablist` (For you, Trending, Communities, Topics) with arrow-key movement and an underline, and the post-type filter as a shared ActionMenu on a chip trigger that names the active type; the desktop dropdown and the hand-rolled phone bottom sheet are gone. Topics is a list of rows without rank badges (count and "this week" as quiet meta). States use the shared feed states: alert with Try again, per-tab empty copy with a "Show all types" escape when a type filter hides everything, skeletons, end line. The tag page gets a back control, the tag as the title, the count as a lede, related tags as chips, and guest/member empty states. Both pages reuse `getPostTypePhrase` and the default avatar instead of a duplicated label table and a missing `/default-avatar.png`.
- **Findings addressed:** UX-01 follow-through on discovery pages; a real bug: search tag results linked to `/explore?tag=` which Explore never read, so a tag search landed on an unfiltered page.
- **Existing capabilities checked:** `useExplore` tabs, type filters, pagination, refresh and auto-retry untouched; `useTagPosts`/`useTrendingTags` untouched; PostCard actions preserved; Explore's ranking and recommendation behaviour untouched. No URL query state existed before and none was added.
- **Evidence:** 4 tests in `components/explore/__tests__/explore.test.tsx` (TabRow semantics and keys; Explore tabs + type menu; filtered empty state escape; error retry and Topics rows); full suite 233 passing; `tsc` clean; ESLint warnings only. Browser (signed in, 1380px): Explore tabs, type menu (computed: fixed, z 1100, opaque), Topics, `/tag/color`. Playwright guest `/explore` and `/tag/poetry` at iPhone 12 / 768 / 1280 with no horizontal overflow.
- **Untested:** a data set with trending tags (the local database returns none, so Topics shows its empty state); Communities tab with real community posts; real keyboard traversal of the tab row in a browser.
- **Decisions:** a type filter is shown as "For you" in the tab row plus the named chip, not as a fifth tab; Explore is a 690px reading column like Classic Home.
- **Revert boundary:** the files above; restoring `ExplorePageContent.tsx`, the tag page/layout and `SearchResultItem.tsx` from `e509c5a` and deleting `Tabs.tsx`/`tabs.css` (and its import) returns the old pages.

## Batch 4A — post detail page and modal — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/post/[id]` and the post modal opened from any feed. New shared seams in `components/feed/PostDetail/` (`PostDetailHeader`, `PostDetailBody` with `JournalHeader` and `MediaGallery`, `PostDetailActions`, `Discussion`, `types.ts` with the one `DetailPost` shape and background tone helper) plus `components/feed/post-detail.css`. `app/post/[id]/page.tsx` and `components/feed/PostDetailModal.tsx` keep all of their data fetching, hooks and handlers and now render only through those seams; roughly 1,100 lines of duplicated markup are gone. The legacy phone override block (`.post-detail-*`, `.post-actions-bar`, `.discussion-panel` mobile rules, ~400 lines of `!important`) was removed from `globals.css`; `.view-discussion-btn` stays until Takes (4C) stops using it.
- **What changed:** both surfaces share the card language from 3B: creator row (avatar, name, flair, type chip, time), the work (journal header on tokens with no gradient text, music embed, sound player, title, text honouring creator alignment/spacing/drop cap/background, media stage with quiet arrows, captions and thumbnails with `aria-current`), tags, and the same `.actions` row as the feed card. The content warning blurs the work and offers "Show the work" on a shared Button. The conversation is one component: a count, the thread, one field with a send control, or a Sign in link for guests. The page adds a Back control (history back, or Home) and lays the work and the conversation side by side from 1024px with a sticky conversation card. The modal shows a Conversation chip on desktop that opens a side panel and, on phones, a full-screen panel with a Back control; Close sits top-right on desktop and Back top-left on phones. The wide dialog now sizes to its content (18rem floor, 90dvh cap) instead of always filling the viewport. Both hand-rolled Block dialogs became `ConfirmationModal`; the page's delete copy matches the card's.
- **Back/close contract:** page: the Back control returns to the previous page, or Home when the page was opened directly; the browser Back also works. Modal: Escape, the scrim, Close (desktop) or Back (phone) all call the provider's close, which restores the underlying URL; inside the modal, creator, tag and mention links close it before navigating.
- **Findings addressed:** UX-09 (seams extracted from two 1,300-line files), the Unsplash fallback avatars in headers and comment fields, the missing `/default-avatar.png` path, the `!important` phone override cascade.
- **Existing capabilities checked:** comments (add, like, reply, load replies, delete, moderator delete), deep-link comment highlight effect (page), reactions, relays (hidden for the owner), saves, share, DM share (card), menu (share, copy link, edit, delete, block, report, pin/unpin via card, remove-me-as-collaborator via modal), collaborations, mentions, hashtags, content warnings, journal metadata, music embed, audio player, creator backgrounds, failed-upload notice via `?media_failed`, auth gating at the point of action.
- **Evidence:** 4 tests in `components/feed/PostDetail/__tests__/postDetail.test.tsx` (actions gating for guest/owner and share; conversation guest prompt and Enter submit; content-warning reveal and media stepping; header links and slots); full suite 237 passing; `tsc` clean; ESLint warnings only. Browser (signed in, 1380px): page for a poem, modal from Home for a quote, Conversation panel, Escape restores `/` and unlocks scroll. Playwright guest: page at iPhone 12 / 768 / 1280 with no overflow; modal on phone (full screen, Back) and desktop (side panel).
- **Untested:** posts with images or video in the redesigned gallery (local data is text-only), creator backgrounds in the new tone mapping, journal entries with weather/mood, a post with comments in the new thread styling, real keyboard traversal of the gallery.
- **Decisions:** the quote form in detail stays plain text for now (the card's quote styling comes from `FormBody`, which is card-specific; unifying it is 4B territory). The modal keeps its existing "conversation hidden until asked" behaviour on desktop.
- **Revert boundary:** `components/feed/PostDetail/`, `components/feed/post-detail.css` and its import, `app/post/[id]/page.tsx`, `components/feed/PostDetailModal.tsx`, the `.pq-dialog--wide` sizing in `overlay.css`, and the removed block in `app/globals.css` (restore from `e509c5a`).

## Batch 4B — composer — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/create` (new and edit). `components/create/CreatePost.tsx` render rewritten (about 2,200 lines of markup became about 1,100) around the untouched state, hooks, draft/upload/publish logic; new `components/create/pieces.tsx` (`ComposerSteps`, `Disclosure`, `Switch`, `FieldLabel`) and `components/create/composer.css`; `components/ui/PeoplePickerModal.tsx` now renders inside the shared `Sheet` (its own overflow/Escape handling removed, gradient pills replaced by tokens); `app/(feed)/create/page.tsx` loading state on the frame. The Take editor markup inside the composer is carried over verbatim for 4C.
- **What changed:** the page sits on the reading frame with a sentence-case title and lede; Post/Take is a segmented control; the two steps are "Write" and "Format & share" with a quiet progress line. Title and body are real labelled fields (`role="textbox"`, `aria-labelledby`) with one line border instead of gradient frames. The formatting toolbar is a `role="toolbar"` of 36px tools (44px on touch) with pressed states; font, text colour and highlight open token-styled popovers. Photos/video use a labelled dropzone, a media grid with numbered items, captions with accessible names, and an "Add more" tile; sound is a row dropzone. Format choice is four labelled cards (Text, Photo, Video, Audio) with `aria-pressed` and the formats of the open category as chips; format details sit in a tinted panel with real labels. Extras are disclosure rows (Soundtrack, Place, Collaborators, Tag people, Content warning) with a status word when set, a real `role="switch"` for the warning, and chips for presets; tags are chips in one field. Visibility and community became shared ActionMenus on chip triggers opening upward; Back / Save draft / Next / Publish are shared Buttons with the primary reserved for the forward action. Draft recovery is a note card with three buttons. Character count and errors use shared classes.
- **Existing capabilities checked (present, not exercised end-to-end):** drafts and recovery, edit mode, collection selector, all formatting commands, fonts/colours/highlights, alignment/spacing/drop cap/background picker, media add/remove/caption, sound attach/remove, format registry (categories, formats, per-format details: music metadata + cover, attribution, subtitle, journal panel), soundtrack (Spotify URL), place, collaborators and tag people via the picker (max counts, exclusions), content warning + presets, tags (20), visibility, community + flair, publish/update, Take mode and its upload progress.
- **Evidence:** 2 tests for the composer pieces (steps, disclosure, switch); full suite 239 passing; `tsc` clean; ESLint warnings only (pre-existing unused Take state). Browser (signed in, 1380px): step 1 with the draft note, toolbar, editor, media; validation alert when Next is pressed with no words; step 2 with format cards, chips, extras, content-warning switch, Collaborators picker on Sheet, visibility menu; test text removed afterwards so no draft was left. No post was published.
- **Untested:** an actual publish from the redesigned composer (deliberately not run from the owner's session); edit mode; Take mode visuals (unchanged markup inside the new frame); phone layout of the composer (needs a signed-in phone session or fixtures); the text colour/highlight popovers open state; drag-and-drop.
- **Decisions:** no live preview pane was added; Step 2 keeps the editor mounted above the format choice as before. The Take editor keeps its old styling until 4C.
- **Revert boundary:** `components/create/CreatePost.tsx`, `components/create/pieces.tsx`, `components/create/composer.css` and its import, `components/ui/PeoplePickerModal.tsx`, `app/(feed)/create/page.tsx`.

## Batch 4C — Takes and pending collaborations — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/takes` (`components/takes/TakesFeed.tsx`, `TakeCard.tsx`, `TakeCommentsPanel.tsx`), `/take/[id]` (`app/take/[id]/page.tsx`), the Take modal (`components/takes/TakeDetailModal.tsx`, opened from studio grids and feed cards), `/takes/create` (`components/takes/CreateTake.tsx`), the Take editor inside `/create` (`components/create/CreatePost.tsx`), `/pending-collaborations` (`app/(feed)/pending-collaborations/page.tsx` + `pending.css`), and `components/ui/ShareModal.tsx`. New: `components/takes/TakeStage.tsx` (the video stage shared by page and modal), `components/takes/TakeEditorPieces.tsx` (dropzone, preview, filter/speed/ratio choices, ranges, cover choice, sound row and list shared by both editors), `components/takes/takes.css` rewritten (about 480 lines replace about 3,400 lines of Takes CSS that lived in `globals.css` and the old file). Deleted: `components/takes/TakeComments.tsx` (unused), the legacy `MODAL`/`SHARE MODAL`/`TAKES*` blocks in `globals.css` (`.view-discussion-btn`, `.discussion-panel`, `.modal-*`, `.tiktok-comment*`, `.take-comments-*`, sound-picker and create-take classes that nothing rendered); `.highlight-comment` moved to `post-detail.css`; `spin`/`cardFadeIn` keyframes re-homed.
- **Seams extended:** `PostDetailHeader` takes `typeLabel` (Takes say "shared a take"), `PostDetailActions` takes `reactionControl` (the Take picker in compact `.action-btn` form), `Discussion` takes `thread` + `count` (Takes render `TakeCommentItem`), `Sheet` takes `initialFocus`. The Take page and modal now render only through these seams plus `TakeStage`; the modal uses the same `.pq-detail-modal` layout and Conversation chip/panel as the post modal.
- **What changed:** immersive feed keeps its snap-scroll and controls but sits on the canvas with a plain 9:16 frame (no aura blobs, gradient arrows, glows or heavy shadows); previous/next are quiet icon buttons; the phone Back control is a 44px scrim circle in the corner; the right rail uses tint circles with the action tone for pressed states (saves and relays no longer amber/green); every rail control has a name and `aria-pressed`. Content warnings veil the frame with "Show the take" (shared with the stage). Comments open in the shared Sheet (trailing panel on desktop, full screen on phones) with the same `TakeCommentItem` the page and modal use and the compose row from `Discussion`; the compose field takes focus. The Take page is the post page's layout: Back to Takes, creator row with Follow (shared Button) and a menu, the stage beside the caption and tags, the quiet action row, and the conversation card. `/takes/create` is the composer's reading frame: dropzone, preview with badges, caption, tags as chips, and disclosures for Look (filter swatches + speed), Sound (row, start, volumes, allow-use switch, search list with preview), Frame, Cover, People and Community/warning (a real `<select>` replaces the custom list), with a Cancel/Post footer and a progress line while uploading. The composer's Take mode uses the same pieces. Pending collaborations is a narrow frame with a sentence-case header, a note when arriving after publishing, a list of drafts with type chip, date, counts and Edit/Open buttons. The share dialog is a Sheet with a Link / Story image / Embed tab row, one copy control, and social buttons on tint circles. Feed take cards use the default avatar, the shared block confirmation, monochrome type and duration pills, and a relayed line in the subdued tone. All four hand-rolled Block dialogs (page, feed card, comment item, comments panel report) became `ConfirmationModal`/`ReportModal`.
- **Bugs fixed on the way:** `/take/[id]` set "Take not found" or "private account" while auth was still loading and never cleared the error on the successful re-fetch, so every take rendered the error card; it also counted saves/relays with `select("id")` on tables that have no `id` column (400s). It now waits for auth, clears the error, selects `take_id`, and shows the real visibility reason on the card.
- **Existing capabilities checked:** immersive playback (snap, active card, mute/volume, keyboard), reactions, comments (open panel, thread, reply, like, delete, report), save, relay, follow, share, copy link, not-interested, delete/report via menu, content warnings, sound rows and "Use" link, take page and modal actions, collaborators/mentions/tags display, create form state (video validation, filters, speed, sound search/preview/start, volumes, allow sound use, aspect ratio, cover from frame or upload, tags, community, warning, collaborators/tag people pickers, upload progress), composer Take mode switch, pending list counts and links.
- **Evidence:** 5 new tests (`components/takes/__tests__/takeStage.test.tsx`: time format, controls, warning veil; `postDetail.test.tsx`: `typeLabel`/`reactionControl` slots, `thread` + `count`); full suite 244 passing; `tsc` clean; ESLint warnings only (pre-existing unused Take state, `<img>` hints). Browser (signed in, 1426px): `/takes` desktop rail and arrows, comments panel over the video with focus in the compose field, `/take/[id]` for a take with a comment (Default and Noir), Share sheet, Take modal from a studio grid with the Conversation panel, `/takes/create` with Look and Sound open, `/create` switched to Take, `/pending-collaborations?created=1`. Playwright guest: `/takes` at iPhone 12 / 768 / 1280 with no horizontal overflow (create and pending redirect guests to sign in). No take was posted, deleted, blocked or reported.
- **Untested:** a take with real footage or a content warning in the new stage (local takes are black frames without warnings), the sound picker with results, upload and post from either editor, phone layout of the Take page/modal/creator (needs a signed-in phone session), the studio Takes grid on real thumbnails, drag-and-drop.
- **Decisions:** the immersive feed stays dark-on-canvas rather than becoming a card list; the Take modal autoplays as before; the Take reaction picker keeps its own icon set (it is a separate reaction table); `SendToDMModal` does not exist in the codebase, so nothing to migrate. Pre-existing and left alone: `/takes` lists takes from private accounts the viewer follows while `useTakes` and the detail page apply slightly different visibility rules; the composer's draft note still shows in Take mode.
- **Revert boundary:** the files above plus `components/feed/PostDetail/{Discussion,PostDetailActions,PostDetailHeader}.tsx`, `components/feed/post-detail.css`, `components/create/composer.css` (Take editor block), `components/ui/overlay.css` (share block), `components/ui/Sheet.tsx`, and the removed blocks in `app/globals.css` (restore from `e509c5a`; restoring also brings back `TakeComments.tsx`).

## Batch 5A — Communities directory and community home — implemented and verified (2026-09-05, baseline `e509c5a`)

- **Routes and components:** `/community` (`app/community/page.tsx`), `/community/[slug]` home (`app/community/[slug]/page.tsx`) and its layout client (`app/community/[slug]/CommunityLayoutClient.tsx`, the identity + gates for every slug page), `/community/create` (`app/community/create/page.tsx`), `components/communities/{CommunityCard,CommunityHeader,JoinButton,TimeRangeDropdown}.tsx`. New: `lib/communities/categories.ts` (one taxonomy: 15 categories with stored name, chip label and genres, plus themes and purposes, and count helpers), `components/communities/communities.css`, `CommunityMark` (avatar or first letter on the action tint) and `membershipWord` exported from the card. Deleted from `globals.css`: `.community-tabs`, `.community-tab`, `.community-member-badge` (unused) and the `.community-card` fade-in stagger; `.community-post-meta` stays (the feed card uses it).
- **What changed:** the directory is a wide frame with a sentence-case header and one "Start a community" button; a search field and a Trending / New / Most members segmented sort on one row; Discover / Joined / Started by you as a tab row for members; every category as a chip in one scrolling row (All plus 15, no hard-coded spotlight); "Busy this week" (three trending cards, only in the default view), "For your kind of work" (suggested, signed in), then "All communities" or "Results" with a count and a Clear control; empty and error states on the shared state card. Cards are quiet: mark, name, Public/Private, member count, two lines of purpose and a single membership word ("Joined", "You moderate", "You run this", "Requested", "Invited"); no cover gradients, rank badges, hover lift or glow. The community home lost the 288px hero banner: a low cover band when there is a cover, then mark, name, description and one meta line (Public/Private community · members · purpose · since), with Share here (active members), the membership control, and a menu; Posts / About / Members (count) / Settings (staff) as a tab row of links with `aria-current`. Under it, the feed on the 3B card language with a Newest / Lively / Top segmented sort (Top adds a time-window menu on the shared ActionMenu), a refresh icon button, "Pinned by the moderators" as a plain label, the welcome message for members as a tinted note, and a compact aside: what it's about (tag chips) and the first five rules with a link to About. Not-found, banned and private-without-membership are shared state cards; the private gate shows the mark, the description, counts and the same membership control. Join is one control: Join / Request to join (primary), Joined / Requested (secondary, leaving or withdrawing asks first on the shared confirmation), Accept invite / Decline, and a disabled Admin label; the request note is a Sheet. Create is the composer's reading frame with three steps (Basics; What it's about; Rules and create), real labels and fields, Public/Private as two choice cards, category / genre / theme / purpose as chips with "add your own" fields, rules as numbered rows, a summary list and a Back / Next / Create footer; it waits for auth before showing the sign-in card.
- **Findings addressed:** UX-04 (banner and repeated sidebar stats), UX-06 (two category lists reconciled into one module; the directory now filters on the stored topic name for every category, including the three the create flow had and the directory lacked).
- **Existing capabilities checked:** discover / joined / created lists, search, category filter, three sorts, trending and suggested sections, join, leave (confirmation opened and dismissed, membership unchanged), request, cancel request, accept/decline invitation (code path only), community menu (copy link, invite, settings, report), posts feed with pin/unpin and moderator delete wiring, Top time window, refresh, welcome message, rules from the shared context, tags, private gate, banned gate, create flow through step 2 (nothing submitted).
- **Evidence:** 3 new tests (`components/communities/__tests__/communities.test.tsx`: taxonomy uniqueness and lookups, card name/count/state, membership words); full suite 247 passing; `tsc` clean; ESLint warnings only (`<img>` hints, pre-existing). Browser (signed in, 1456px): `/community` with tabs and sections, `/community/pinkquill` as admin (welcome note, pinned post, Top + time menu, Noir), `/community/writers-in-jeddah` as member with the Leave confirmation, `/community/kojr-pops` private gate showing Requested, `/community/create` steps 1 and 2. Playwright guest: `/community` and `/community/create` at iPhone 12 / 768 / 1280, no horizontal overflow.
- **Untested:** a community with a cover image (none locally), busy communities with many posts and pinned items, the invitation Accept/Decline buttons live, an actual create or leave (deliberately not run from the owner's session), moderator role views, phone layout signed in.
- **Decisions:** the directory keeps the existing trending/suggested hooks unchanged and drops only the hard-coded four-category spotlight; "Hot" is labelled "Lively" and "Popular" is "Most members" (same sorts, plainer words); the About/Members/Settings pages keep their old markup under the new header until 5B.
- **Revert boundary:** the files above plus the removed blocks in `app/globals.css` (restore from `e509c5a`).

## Batch 5B — community membership, rules, moderation, settings — implemented and verified (2026-09-06, baseline `e509c5a`)

- **Routes and components:** `/community/[slug]/about`, `/community/[slug]/members`, `/community/[slug]/mod`, `/community/[slug]/settings` and every tab under it (`general`, `rules`, `chat`, `flairs`, `members`, `moderation`); `components/communities/{InviteModal,ModeratorPermissionsModal,FlairManager}.tsx`, `components/communities/ModQueue/{ModQueuePage,ReportCard}.tsx`. New: `components/communities/pieces.tsx` (`PersonRow`/`PersonAvatar`/`personName`/`roleWord`/`formatDay`, `settingsSections`, `CommunitySettingsFrame`, `Notice`), `components/communities/ModerationSheet.tsx` (one mute/ban sheet), and the people/settings/report/log rules appended to `communities.css`. The members page render went from about 1,070 lines to about 415 around unchanged hooks and handlers.
- **One local navigation:** every staff surface (Overview, General, Rules, Chat, Flairs, Roles and requests, Moderation log, Reports) renders inside `CommunitySettingsFrame`: a sticky side list from 1024px, a scrolling tab row below, with `aria-current` on the open section and admin-only sections hidden from moderators. Reports (`/mod`) lives in the same frame instead of its own breadcrumb page. The overview is a list of rows with one-line descriptions and, for admins, a separate danger card whose delete asks for the community name on a Sheet.
- **What changed:** About is two calm cards (description, numbered rules with an Edit link for staff) plus tags grouped as chips, a Details list and a "Run by" list of people. Members is a search field and an Invite button, an "Asking to join" list for staff of private communities with Let them in / Decline, Members / Muted / Banned tabs with counts, role chips, and one `PersonRow` per person with the role or status word beside the name and a real ActionMenu (Make moderator, Change what they can do, Remove moderator role, Mute, Ban) instead of the hover-only dropdown; Unmute and Lift ban are plain secondary buttons. Mute and ban share `ModerationSheet`: duration chips (with a custom hours field), a reason the person will see, and one danger button; removing a moderator role and lifting a ban use the shared confirmation. Invitations and moderator permissions moved onto Sheets: the permissions sheet is seven labelled switches with "Turn all on / off" and a count. General uses real fields, a cover band preview with an "Add a cover" button, the community mark with "Add a mark", and Public/Private as choice cards. Rules are editable numbered rows with drag handles and a remove button, plus an "Add a rule" panel; Chat is the welcome message plus three labelled switches (read-only words for moderators). Flairs keeps its colour chips, emoji and preview but on shared fields, a list with move up/down and Edit/Delete, and a confirmation before delete. Moderation log entries read as sentences ("Hadi removed a post by @poet") with the snapshot and reason; muted and banned lists say until when and why. Reports shows two plain stat tiles, an Open / Reviewed / Resolved / All segmented filter and kind chips, and each report as a card: who reported what, the reason, the content, and a "Decide" flow with four labelled outcomes ordered lightest first (warning, remove, mute, ban), a note, and a danger button only for the two destructive outcomes.
- **Bugs fixed on the way:** the reports queue never loaded (`useModQueue` joined `posts!reported_post_id` and filtered on `reported_post_id`, but the `reports` table column is `post_id`; the hook now joins on `post_id` and maps it onto `reported_post_id` for the UI). The roles page called `router.push` during render; it now redirects from an effect like its siblings. The members page's action menu was hover-only (unreachable by keyboard and touch); it is now a real menu.
- **Existing capabilities checked:** join requests (approve/decline wiring), invite search, promote with permissions, edit permissions, demote, mute/unmute, ban/unban, role filters and search, moderator/admin gates, general save with uploads, rules add/edit/reorder/remove/save, chat welcome + toggles, flair create/edit/delete/reorder, mod log paging, report filters/stats/resolve/dismiss, community delete with typed confirmation. None were executed against live data.
- **Evidence:** 2 new tests in `components/communities/__tests__/communities.test.tsx` (settings sections and role words; `PersonRow` name link, handle, word, meta); full suite 249 passing; `tsc` clean; ESLint 0 errors (warnings are `<img>` hints). Browser (signed in as the admin of PinkQuill, 1456px): About, Members with the action menu and the mute sheet (opened and dismissed with Escape, nobody muted), Settings overview, General, Rules, Chat, Flairs, Roles and requests (Noir too), Moderation log with real entries, Reports (empty, loads after the fix). Playwright guest: About and Members at iPhone 12 / 768 / 1280 with no horizontal overflow.
- **Untested:** a private community with pending requests, a moderator's (non-admin) view of settings, live mute/ban/promote/invite/flair/delete actions, a queue with open reports, phone layout of the settings frame signed in, drag-reorder of rules by keyboard.
- **Decisions:** "Mod queue" is called Reports and "Moderation" is called Moderation log so the two stop reading alike; Reports keeps its `/mod` URL. Ban and delete keep their danger colour only on the final button. The `_handlePromote`/`_handlePromoteToAdmin` reserved handlers were left as they were.
- **Revert boundary:** the files above plus `components/ui/tabs.css` (`aria-checked` chip state) and `lib/hooks/useModQueue.ts` (restore from `e509c5a`).

## Batch 5C — DMs, community inbox, notifications — implemented and verified (2026-09-06, baseline `e509c5a`)

- **Routes and components:** `/messages` (list, thread, composer, attachments, voice notes, phone back behaviour), `/messages/community` (`components/messages/community/CommunityInboxView.tsx`), the notification panel and its cards (`components/notifications/{NotificationPanel,FollowRequestCard,CollaborationInviteCard}.tsx`), `components/messages/{MessagesView,ConversationList,ChatView,NewMessageModal,SendToDMModal,TypingIndicator,VoiceNotePlayer,VoiceRecorder}.tsx`, `app/(feed)/messages/{page,loading}.tsx`, `app/(feed)/messages/community/page.tsx`. `components/messages/messages.css` was rewritten on the tokens (list rows, thread head, day dividers, bubbles, read marks, reactions, composer, media preview, voice player and recorder); `components/notifications/notifications.css` is new. The VOICE NOTES and COLLABORATION INVITE CARD blocks left `globals.css`.
- **One conversation frame:** `.pq-msgs` holds a list pane and a thread pane; `data-open="list" | "thread"` is the only phone state, so the back control in the thread head and the row click are the whole navigation. DMs and the community inbox share the row (`.pq-thread-row`, `aria-current` on the open one, an unread count only when there is one), the head, the log, the bubbles and the composer; the community inbox keeps its third pane (community chat plus member threads with a member search), the Message / Announcement segmented control with an "Everyone in the chat can see this" line, and the appeal chip for muted members. Announcements are a labelled bubble, not a coloured banner.
- **What changed:** the list is a plain heading, a community-inbox icon button with a count and one "New" button; rows are avatar, name, when, a one-line preview ("You:" prefix, "Voice note · 0:07", "Photo", "Video", "No messages yet") and the unread count. The thread head is the person (name, handle, link to their studio) and one ActionMenu (view studio, block, report, delete). Messages are calm bubbles: own on the action tint to the right, theirs on the surface to the left, sender name only in group chats, time and read ticks in a small foot, reactions as a row under the bubble, day dividers as pills. The composer is one field with attach, emoji and microphone inside it, a round send button, and a hint line for the blocked/muted/off states; media previews sit above it with a remove button. Voice notes are a play button, a waveform in the action colour over the line colour, the time and a speed chip; recording is a soft danger dot, the elapsed time (turning to the danger ink near the limit) and Stop. The empty thread pane says "Your messages" with a "New message" button. New message and Send to DM are Sheets on `PersonRow` with a search field and, for sending, chips for the chosen people. Report is the shared `ReportModal`; block and delete are the shared confirmation. The notification panel keeps the Sheet panel and is now a list of rows with a monochrome kind mark on the avatar (heart, comment, relay, follow, order, message, system), the actor in the ink colour, the action in the subdued colour, a one-line preview and the time; unread rows carry the tint; sections are "Asking to follow you", "Collaboration invites" and "Activity"; the footer links to `/settings/notifications`. Follow requests and invites are `PersonRow` cards with Accept / Decline buttons.
- **Bugs fixed on the way:** `ChatView` had its own hand-rolled report dialog; it now uses `ReportModal` (`handleReport(reason, details)`). The waveform colours were hard-coded hex/rgba; they are tokens now. `CollaborationInviteCard` typed its handlers as `Promise<void>` while the hook returns a result object; the panel wraps them. The messages route loading states were spinners on legacy classes; they are the shared Spinner.
- **Existing capabilities checked:** conversation overview RPC, unread counts, block state (composer off), typing indicator, read receipts, reactions picker, media send and lightbox, voice record/play/speed, shared post cards, delete/block/report, community chat vs member threads, announcements (staff only), mute appeal, notification mark-as-read on open, follow request accept/decline, collaboration invite accept/decline, muted notification types. None were executed with live mutation.
- **Evidence:** 3 new tests in `components/messages/__tests__/messages.test.tsx` (rows with unread words, "You:" and voice preview, `aria-current`, click; empty state; typing status); full suite 252 passing (7 skipped); `tsc` clean; ESLint 0 errors (20 warnings, all pre-existing `<img>`, unused vars and exhaustive-deps hints). Browser (signed in as hadi, 1456px): `/messages` list and empty pane, the poet thread (text bubbles, a voice note, read ticks, day dividers, composer), `/messages/community` (PinkQuill chat with the welcome bubble, two announcements, a member's message, Message / Announcement control) and the notification panel from Home with real activity rows; the list, the thread and the panel again in Noir. Guest Playwright shots do not apply: both routes redirect to sign-in.
- **Untested:** sending a message, recording, attaching media, reacting, phone pane switching signed in, an inbox with unread rows, a muted member's appeal chip, the follow-request and invite sections (nothing pending), Send to DM from a post, the community inbox for a member who is not staff.
- **Decisions:** no colour on notification kinds (the mark is monochrome; the unread row carries the tint). Read ticks stay text-sized and subdued; nothing pulses. The community inbox keeps three panes on desktop because member threads and the shared chat are different things.
- **Browser-check gotcha:** the first synthetic click after a navigation in the Chrome tool does not always reach React (the bell's `aria-expanded` stayed false); a DOM `.click()` works first time and a second tool click works. Re-click before treating a missed open as a bug.
- **Revert boundary:** the files above (restore from `e509c5a`; `notifications.css` and the test file are new).

## Exact next task: 6A public studio

Read `03-phases.md` §6 and the 5C entry above. Redesign `/studio/[username]` (`app/studio/[username]/*`, `components/studio/*`): rebalance the cover, identity, bio, social actions, metrics and the work on the shared frames and tokens; use the existing fields and tabs (`TabRow`), and preserve the owner / visitor / private / blocked states. Follower and following dialogs go on `Sheet` with `PersonRow` from `components/communities/pieces.tsx`. Show a convincing text-only studio and a mixed-media studio. Move studio CSS out of `globals.css` as touched into `components/studio/studio.css`. Own-profile management must read clearly apart from public content; a studio without a shop or commissions must not feel incomplete.

## Open verification

- Native dialog Tab/Shift+Tab boundary; screen reader; 200% zoom; OS reduced motion; phone software keyboard.
- Authenticated creator/member/moderator/buyer/seller/admin integration journeys and actual preference persistence were not exercised with live mutation. Use controlled fixtures or designated test sessions.
- Full composer/upload/media behavior and financial server eligibility remain owned by their later phases.
- Other primitive themes and global consumer-specific overrides need verification as they migrate.
- The user approved proceeding with the plan; there is no pending permission request and no claim of final pixel approval or production release readiness.

## Batch entry template

Copy this for each future entry:

```text
Date / baseline commit:
Batch ID and status (in progress / implemented / verified / complete):
Routes and components:
Findings addressed:
Existing capabilities checked:
Files changed:
Preview / screenshot evidence:
Tests and manual checks (exact result):
Untested states / blockers / risks:
Decisions and user feedback:
Revert boundary:
Exact next task:
```

“Implemented” means code exists. “Verified” means the stated checks passed. “Complete” additionally means route coverage and handoff are updated and no required work remains for that batch. Never infer a deployed result from any of these labels.
